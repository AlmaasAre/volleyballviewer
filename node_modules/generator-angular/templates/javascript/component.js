'use strict';

angular.module('<%= _.camelize(appname)%>App.components')
	.controller('<%= _.camelize(name)%>ComponentCtrl', function <%= _.camelize(name) %>ComponentCtrl($scope, $attrs) {

		$scope.test = $attrs.test;

	})
	.directive('<%= _.camelize(name)%>Component', function (componentFactory, $log) {

			var _linkFn = function postLink(scope, element, attrs) {
				$log.log('<%= name %> component loaded');
			}

			return componentFactory.createComponent('<%= _.camelize(name) %>Component', {
				scope: {
					message: '@',
					value: '='
				},
				link: _linkFn,
				controller: '<%= _.camelize(name)%>ComponentCtrl'
			});
	});
